apiVersion: v1
kind: Service
metadata:
  name: openauth
spec:
  selector:
    app: openauth
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: grpc
      port: 8081
      targetPort: grpc
