apiVersion: v1
kind: Service
metadata:
  name: openauth
spec:
  selector:
    app: openauth
  ports:
    - name: http
      port: 8085
      targetPort: http
    - name: grpc
      port: 8086
      targetPort: grpc
