version: '3.8'
services:
  openauth:
    image: openauth:latest
    container_name: openauth
    restart: always
    ports:
      - "8085:8085"
      - "8086:8086"
    volumes:
      - .:/app
    environment:
      EXAMPLE_ENV: value
