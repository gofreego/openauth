# OpenAuth Environment Variables
# Copy this file to .env and update the values for your environment

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-256-bit-jwt-key-change-this-in-production-environments

# Database Configuration (optional - can be overridden via config file)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=OSwfiTlc1r5W7Z
DB_NAME=openauth
DB_SSL_MODE=disable

# Server Configuration
HTTP_PORT=8085
GRPC_PORT=8086

# Security Settings
BCRYPT_COST=12

# Debug Settings
DEBUG_ENABLED=true
ENABLE_PPROF=true
