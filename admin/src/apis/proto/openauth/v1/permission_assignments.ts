// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: proto/openauth/v1/permission_assignments.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "v1";

/** Request to assign a permission to a group */
export interface AssignPermissionsToGroupRequest {
  /** ID of the group to assign the permission to */
  groupId: string;
  /** IDs of the permissions to assign */
  permissionsIds: string[];
}

/** Response after assigning a permission to a group */
export interface AssignPermissionsToGroupResponse {
  message: string;
}

/** Request to remove a permission from a group */
export interface RemovePermissionsFromGroupRequest {
  /** ID of the group to remove the permission from */
  groupId: string;
  /** IDs of the permissions to remove */
  permissionsIds: string[];
}

/** Response after removing a permission from a group */
export interface RemovePermissionsFromGroupResponse {
  success: boolean;
  message: string;
}

/** Request to list permissions assigned to a group */
export interface ListGroupPermissionsRequest {
  /** ID of the group to list permissions for */
  groupId: string;
}

/** Response containing group permissions */
export interface ListGroupPermissionsResponse {
  /** Array of permissions assigned to the group */
  permissions: EffectivePermission[];
}

/** Request to assign a permission directly to a user */
export interface AssignPermissionsToUserRequest {
  /** ID of the user to assign the permission to */
  userId: string;
  /** IDs of the permissions to assign */
  permissionsIds: string[];
  /** Optional expiration timestamp (null means no expiration) */
  expiresAt?: string | undefined;
}

/** Response after assigning a permission to a user */
export interface AssignPermissionsToUserResponse {
  message: string;
}

/** Request to remove a permission from a user */
export interface RemovePermissionsFromUserRequest {
  /** ID of the user to remove the permission from */
  userId: string;
  /** IDs of the permissions to remove */
  permissionsIds: string[];
}

/** Response after removing a permission from a user */
export interface RemovePermissionsFromUserResponse {
  success: boolean;
  message: string;
}

/** Request to list permissions assigned directly to a user */
export interface ListUserPermissionsRequest {
  /** ID of the user to list permissions for */
  userId: string;
}

/** Response containing user permissions */
export interface ListUserPermissionsResponse {
  /** Array of permissions assigned to the user */
  permissions: EffectivePermission[];
}

/** Request to get all effective permissions for a user (direct + group permissions) */
export interface GetUserEffectivePermissionsRequest {
  /** ID of the user to get effective permissions for */
  userId: string;
}

/** EffectivePermission represents a permission that a user has either directly or through group membership */
export interface EffectivePermission {
  /** Permission details */
  permissionId: string;
  permissionName: string;
  permissionDisplayName: string;
  permissionDescription?:
    | string
    | undefined;
  /** Source of the permission: "direct" or "group" */
  source: string;
  /** If source is "group", this contains the group details */
  groupId?: string | undefined;
  groupName?: string | undefined;
  groupDisplayName?:
    | string
    | undefined;
  /** If source is "direct", this may contain expiration info */
  expiresAt?:
    | string
    | undefined;
  /** When this permission was granted */
  grantedAt: string;
  /** Who granted this permission */
  grantedBy: string;
}

/** Response containing effective permissions for a user */
export interface GetUserEffectivePermissionsResponse {
  /** Array of effective permissions for the user */
  permissions: EffectivePermission[];
}

function createBaseAssignPermissionsToGroupRequest(): AssignPermissionsToGroupRequest {
  return { groupId: "0", permissionsIds: [] };
}

export const AssignPermissionsToGroupRequest: MessageFns<AssignPermissionsToGroupRequest> = {
  encode(message: AssignPermissionsToGroupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== "0") {
      writer.uint32(8).int64(message.groupId);
    }
    for (const v of message.permissionsIds) {
      writer.uint32(16).int64(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignPermissionsToGroupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignPermissionsToGroupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.permissionsIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.permissionsIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssignPermissionsToGroupRequest {
    return {
      groupId: isSet(object.groupId)
        ? globalThis.String(object.groupId)
        : isSet(object.group_id)
        ? globalThis.String(object.group_id)
        : "0",
      permissionsIds: globalThis.Array.isArray(object?.permissionsIds)
        ? object.permissionsIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.permissions_ids)
        ? object.permissions_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: AssignPermissionsToGroupRequest): unknown {
    const obj: any = {};
    if (message.groupId !== "0") {
      obj.groupId = message.groupId;
    }
    if (message.permissionsIds?.length) {
      obj.permissionsIds = message.permissionsIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AssignPermissionsToGroupRequest>, I>>(base?: I): AssignPermissionsToGroupRequest {
    return AssignPermissionsToGroupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AssignPermissionsToGroupRequest>, I>>(
    object: I,
  ): AssignPermissionsToGroupRequest {
    const message = createBaseAssignPermissionsToGroupRequest();
    message.groupId = object.groupId ?? "0";
    message.permissionsIds = object.permissionsIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseAssignPermissionsToGroupResponse(): AssignPermissionsToGroupResponse {
  return { message: "" };
}

export const AssignPermissionsToGroupResponse: MessageFns<AssignPermissionsToGroupResponse> = {
  encode(message: AssignPermissionsToGroupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignPermissionsToGroupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignPermissionsToGroupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssignPermissionsToGroupResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: AssignPermissionsToGroupResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AssignPermissionsToGroupResponse>, I>>(
    base?: I,
  ): AssignPermissionsToGroupResponse {
    return AssignPermissionsToGroupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AssignPermissionsToGroupResponse>, I>>(
    object: I,
  ): AssignPermissionsToGroupResponse {
    const message = createBaseAssignPermissionsToGroupResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRemovePermissionsFromGroupRequest(): RemovePermissionsFromGroupRequest {
  return { groupId: "0", permissionsIds: [] };
}

export const RemovePermissionsFromGroupRequest: MessageFns<RemovePermissionsFromGroupRequest> = {
  encode(message: RemovePermissionsFromGroupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== "0") {
      writer.uint32(8).int64(message.groupId);
    }
    for (const v of message.permissionsIds) {
      writer.uint32(16).int64(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePermissionsFromGroupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePermissionsFromGroupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.permissionsIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.permissionsIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePermissionsFromGroupRequest {
    return {
      groupId: isSet(object.groupId)
        ? globalThis.String(object.groupId)
        : isSet(object.group_id)
        ? globalThis.String(object.group_id)
        : "0",
      permissionsIds: globalThis.Array.isArray(object?.permissionsIds)
        ? object.permissionsIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.permissions_ids)
        ? object.permissions_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: RemovePermissionsFromGroupRequest): unknown {
    const obj: any = {};
    if (message.groupId !== "0") {
      obj.groupId = message.groupId;
    }
    if (message.permissionsIds?.length) {
      obj.permissionsIds = message.permissionsIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemovePermissionsFromGroupRequest>, I>>(
    base?: I,
  ): RemovePermissionsFromGroupRequest {
    return RemovePermissionsFromGroupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemovePermissionsFromGroupRequest>, I>>(
    object: I,
  ): RemovePermissionsFromGroupRequest {
    const message = createBaseRemovePermissionsFromGroupRequest();
    message.groupId = object.groupId ?? "0";
    message.permissionsIds = object.permissionsIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseRemovePermissionsFromGroupResponse(): RemovePermissionsFromGroupResponse {
  return { success: false, message: "" };
}

export const RemovePermissionsFromGroupResponse: MessageFns<RemovePermissionsFromGroupResponse> = {
  encode(message: RemovePermissionsFromGroupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePermissionsFromGroupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePermissionsFromGroupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePermissionsFromGroupResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RemovePermissionsFromGroupResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemovePermissionsFromGroupResponse>, I>>(
    base?: I,
  ): RemovePermissionsFromGroupResponse {
    return RemovePermissionsFromGroupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemovePermissionsFromGroupResponse>, I>>(
    object: I,
  ): RemovePermissionsFromGroupResponse {
    const message = createBaseRemovePermissionsFromGroupResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseListGroupPermissionsRequest(): ListGroupPermissionsRequest {
  return { groupId: "0" };
}

export const ListGroupPermissionsRequest: MessageFns<ListGroupPermissionsRequest> = {
  encode(message: ListGroupPermissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== "0") {
      writer.uint32(8).int64(message.groupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListGroupPermissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListGroupPermissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListGroupPermissionsRequest {
    return {
      groupId: isSet(object.groupId)
        ? globalThis.String(object.groupId)
        : isSet(object.group_id)
        ? globalThis.String(object.group_id)
        : "0",
    };
  },

  toJSON(message: ListGroupPermissionsRequest): unknown {
    const obj: any = {};
    if (message.groupId !== "0") {
      obj.groupId = message.groupId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListGroupPermissionsRequest>, I>>(base?: I): ListGroupPermissionsRequest {
    return ListGroupPermissionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListGroupPermissionsRequest>, I>>(object: I): ListGroupPermissionsRequest {
    const message = createBaseListGroupPermissionsRequest();
    message.groupId = object.groupId ?? "0";
    return message;
  },
};

function createBaseListGroupPermissionsResponse(): ListGroupPermissionsResponse {
  return { permissions: [] };
}

export const ListGroupPermissionsResponse: MessageFns<ListGroupPermissionsResponse> = {
  encode(message: ListGroupPermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      EffectivePermission.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListGroupPermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListGroupPermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(EffectivePermission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListGroupPermissionsResponse {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => EffectivePermission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListGroupPermissionsResponse): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions.map((e) => EffectivePermission.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListGroupPermissionsResponse>, I>>(base?: I): ListGroupPermissionsResponse {
    return ListGroupPermissionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListGroupPermissionsResponse>, I>>(object: I): ListGroupPermissionsResponse {
    const message = createBaseListGroupPermissionsResponse();
    message.permissions = object.permissions?.map((e) => EffectivePermission.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAssignPermissionsToUserRequest(): AssignPermissionsToUserRequest {
  return { userId: "0", permissionsIds: [], expiresAt: undefined };
}

export const AssignPermissionsToUserRequest: MessageFns<AssignPermissionsToUserRequest> = {
  encode(message: AssignPermissionsToUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    for (const v of message.permissionsIds) {
      writer.uint32(16).int64(v!);
    }
    if (message.expiresAt !== undefined) {
      writer.uint32(24).int64(message.expiresAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignPermissionsToUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignPermissionsToUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.permissionsIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.permissionsIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresAt = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssignPermissionsToUserRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "0",
      permissionsIds: globalThis.Array.isArray(object?.permissionsIds)
        ? object.permissionsIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.permissions_ids)
        ? object.permissions_ids.map((e: any) => globalThis.String(e))
        : [],
      expiresAt: isSet(object.expiresAt)
        ? globalThis.String(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.String(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: AssignPermissionsToUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.permissionsIds?.length) {
      obj.permissionsIds = message.permissionsIds;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AssignPermissionsToUserRequest>, I>>(base?: I): AssignPermissionsToUserRequest {
    return AssignPermissionsToUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AssignPermissionsToUserRequest>, I>>(
    object: I,
  ): AssignPermissionsToUserRequest {
    const message = createBaseAssignPermissionsToUserRequest();
    message.userId = object.userId ?? "0";
    message.permissionsIds = object.permissionsIds?.map((e) => e) || [];
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

function createBaseAssignPermissionsToUserResponse(): AssignPermissionsToUserResponse {
  return { message: "" };
}

export const AssignPermissionsToUserResponse: MessageFns<AssignPermissionsToUserResponse> = {
  encode(message: AssignPermissionsToUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignPermissionsToUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignPermissionsToUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssignPermissionsToUserResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: AssignPermissionsToUserResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AssignPermissionsToUserResponse>, I>>(base?: I): AssignPermissionsToUserResponse {
    return AssignPermissionsToUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AssignPermissionsToUserResponse>, I>>(
    object: I,
  ): AssignPermissionsToUserResponse {
    const message = createBaseAssignPermissionsToUserResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRemovePermissionsFromUserRequest(): RemovePermissionsFromUserRequest {
  return { userId: "0", permissionsIds: [] };
}

export const RemovePermissionsFromUserRequest: MessageFns<RemovePermissionsFromUserRequest> = {
  encode(message: RemovePermissionsFromUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    for (const v of message.permissionsIds) {
      writer.uint32(16).int64(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePermissionsFromUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePermissionsFromUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.permissionsIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.permissionsIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePermissionsFromUserRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "0",
      permissionsIds: globalThis.Array.isArray(object?.permissionsIds)
        ? object.permissionsIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.permissions_ids)
        ? object.permissions_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: RemovePermissionsFromUserRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.permissionsIds?.length) {
      obj.permissionsIds = message.permissionsIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemovePermissionsFromUserRequest>, I>>(
    base?: I,
  ): RemovePermissionsFromUserRequest {
    return RemovePermissionsFromUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemovePermissionsFromUserRequest>, I>>(
    object: I,
  ): RemovePermissionsFromUserRequest {
    const message = createBaseRemovePermissionsFromUserRequest();
    message.userId = object.userId ?? "0";
    message.permissionsIds = object.permissionsIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseRemovePermissionsFromUserResponse(): RemovePermissionsFromUserResponse {
  return { success: false, message: "" };
}

export const RemovePermissionsFromUserResponse: MessageFns<RemovePermissionsFromUserResponse> = {
  encode(message: RemovePermissionsFromUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemovePermissionsFromUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemovePermissionsFromUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemovePermissionsFromUserResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RemovePermissionsFromUserResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemovePermissionsFromUserResponse>, I>>(
    base?: I,
  ): RemovePermissionsFromUserResponse {
    return RemovePermissionsFromUserResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemovePermissionsFromUserResponse>, I>>(
    object: I,
  ): RemovePermissionsFromUserResponse {
    const message = createBaseRemovePermissionsFromUserResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseListUserPermissionsRequest(): ListUserPermissionsRequest {
  return { userId: "0" };
}

export const ListUserPermissionsRequest: MessageFns<ListUserPermissionsRequest> = {
  encode(message: ListUserPermissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListUserPermissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListUserPermissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListUserPermissionsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "0",
    };
  },

  toJSON(message: ListUserPermissionsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListUserPermissionsRequest>, I>>(base?: I): ListUserPermissionsRequest {
    return ListUserPermissionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListUserPermissionsRequest>, I>>(object: I): ListUserPermissionsRequest {
    const message = createBaseListUserPermissionsRequest();
    message.userId = object.userId ?? "0";
    return message;
  },
};

function createBaseListUserPermissionsResponse(): ListUserPermissionsResponse {
  return { permissions: [] };
}

export const ListUserPermissionsResponse: MessageFns<ListUserPermissionsResponse> = {
  encode(message: ListUserPermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      EffectivePermission.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListUserPermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListUserPermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(EffectivePermission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListUserPermissionsResponse {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => EffectivePermission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListUserPermissionsResponse): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions.map((e) => EffectivePermission.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListUserPermissionsResponse>, I>>(base?: I): ListUserPermissionsResponse {
    return ListUserPermissionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListUserPermissionsResponse>, I>>(object: I): ListUserPermissionsResponse {
    const message = createBaseListUserPermissionsResponse();
    message.permissions = object.permissions?.map((e) => EffectivePermission.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetUserEffectivePermissionsRequest(): GetUserEffectivePermissionsRequest {
  return { userId: "0" };
}

export const GetUserEffectivePermissionsRequest: MessageFns<GetUserEffectivePermissionsRequest> = {
  encode(message: GetUserEffectivePermissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).int64(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserEffectivePermissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserEffectivePermissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserEffectivePermissionsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "0",
    };
  },

  toJSON(message: GetUserEffectivePermissionsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserEffectivePermissionsRequest>, I>>(
    base?: I,
  ): GetUserEffectivePermissionsRequest {
    return GetUserEffectivePermissionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserEffectivePermissionsRequest>, I>>(
    object: I,
  ): GetUserEffectivePermissionsRequest {
    const message = createBaseGetUserEffectivePermissionsRequest();
    message.userId = object.userId ?? "0";
    return message;
  },
};

function createBaseEffectivePermission(): EffectivePermission {
  return {
    permissionId: "0",
    permissionName: "",
    permissionDisplayName: "",
    permissionDescription: undefined,
    source: "",
    groupId: undefined,
    groupName: undefined,
    groupDisplayName: undefined,
    expiresAt: undefined,
    grantedAt: "0",
    grantedBy: "0",
  };
}

export const EffectivePermission: MessageFns<EffectivePermission> = {
  encode(message: EffectivePermission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissionId !== "0") {
      writer.uint32(8).int64(message.permissionId);
    }
    if (message.permissionName !== "") {
      writer.uint32(18).string(message.permissionName);
    }
    if (message.permissionDisplayName !== "") {
      writer.uint32(26).string(message.permissionDisplayName);
    }
    if (message.permissionDescription !== undefined) {
      writer.uint32(34).string(message.permissionDescription);
    }
    if (message.source !== "") {
      writer.uint32(42).string(message.source);
    }
    if (message.groupId !== undefined) {
      writer.uint32(48).int64(message.groupId);
    }
    if (message.groupName !== undefined) {
      writer.uint32(58).string(message.groupName);
    }
    if (message.groupDisplayName !== undefined) {
      writer.uint32(66).string(message.groupDisplayName);
    }
    if (message.expiresAt !== undefined) {
      writer.uint32(72).int64(message.expiresAt);
    }
    if (message.grantedAt !== "0") {
      writer.uint32(80).int64(message.grantedAt);
    }
    if (message.grantedBy !== "0") {
      writer.uint32(88).int64(message.grantedBy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectivePermission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectivePermission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.permissionId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.permissionName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.permissionDisplayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.permissionDescription = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.groupId = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.groupName = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.groupDisplayName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.expiresAt = reader.int64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.grantedAt = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.grantedBy = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EffectivePermission {
    return {
      permissionId: isSet(object.permissionId)
        ? globalThis.String(object.permissionId)
        : isSet(object.permission_id)
        ? globalThis.String(object.permission_id)
        : "0",
      permissionName: isSet(object.permissionName)
        ? globalThis.String(object.permissionName)
        : isSet(object.permission_name)
        ? globalThis.String(object.permission_name)
        : "",
      permissionDisplayName: isSet(object.permissionDisplayName)
        ? globalThis.String(object.permissionDisplayName)
        : isSet(object.permission_display_name)
        ? globalThis.String(object.permission_display_name)
        : "",
      permissionDescription: isSet(object.permissionDescription)
        ? globalThis.String(object.permissionDescription)
        : isSet(object.permission_description)
        ? globalThis.String(object.permission_description)
        : undefined,
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      groupId: isSet(object.groupId)
        ? globalThis.String(object.groupId)
        : isSet(object.group_id)
        ? globalThis.String(object.group_id)
        : undefined,
      groupName: isSet(object.groupName)
        ? globalThis.String(object.groupName)
        : isSet(object.group_name)
        ? globalThis.String(object.group_name)
        : undefined,
      groupDisplayName: isSet(object.groupDisplayName)
        ? globalThis.String(object.groupDisplayName)
        : isSet(object.group_display_name)
        ? globalThis.String(object.group_display_name)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? globalThis.String(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.String(object.expires_at)
        : undefined,
      grantedAt: isSet(object.grantedAt)
        ? globalThis.String(object.grantedAt)
        : isSet(object.granted_at)
        ? globalThis.String(object.granted_at)
        : "0",
      grantedBy: isSet(object.grantedBy)
        ? globalThis.String(object.grantedBy)
        : isSet(object.granted_by)
        ? globalThis.String(object.granted_by)
        : "0",
    };
  },

  toJSON(message: EffectivePermission): unknown {
    const obj: any = {};
    if (message.permissionId !== "0") {
      obj.permissionId = message.permissionId;
    }
    if (message.permissionName !== "") {
      obj.permissionName = message.permissionName;
    }
    if (message.permissionDisplayName !== "") {
      obj.permissionDisplayName = message.permissionDisplayName;
    }
    if (message.permissionDescription !== undefined) {
      obj.permissionDescription = message.permissionDescription;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.groupId !== undefined) {
      obj.groupId = message.groupId;
    }
    if (message.groupName !== undefined) {
      obj.groupName = message.groupName;
    }
    if (message.groupDisplayName !== undefined) {
      obj.groupDisplayName = message.groupDisplayName;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt;
    }
    if (message.grantedAt !== "0") {
      obj.grantedAt = message.grantedAt;
    }
    if (message.grantedBy !== "0") {
      obj.grantedBy = message.grantedBy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EffectivePermission>, I>>(base?: I): EffectivePermission {
    return EffectivePermission.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EffectivePermission>, I>>(object: I): EffectivePermission {
    const message = createBaseEffectivePermission();
    message.permissionId = object.permissionId ?? "0";
    message.permissionName = object.permissionName ?? "";
    message.permissionDisplayName = object.permissionDisplayName ?? "";
    message.permissionDescription = object.permissionDescription ?? undefined;
    message.source = object.source ?? "";
    message.groupId = object.groupId ?? undefined;
    message.groupName = object.groupName ?? undefined;
    message.groupDisplayName = object.groupDisplayName ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.grantedAt = object.grantedAt ?? "0";
    message.grantedBy = object.grantedBy ?? "0";
    return message;
  },
};

function createBaseGetUserEffectivePermissionsResponse(): GetUserEffectivePermissionsResponse {
  return { permissions: [] };
}

export const GetUserEffectivePermissionsResponse: MessageFns<GetUserEffectivePermissionsResponse> = {
  encode(message: GetUserEffectivePermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      EffectivePermission.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserEffectivePermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserEffectivePermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(EffectivePermission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserEffectivePermissionsResponse {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => EffectivePermission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUserEffectivePermissionsResponse): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions.map((e) => EffectivePermission.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserEffectivePermissionsResponse>, I>>(
    base?: I,
  ): GetUserEffectivePermissionsResponse {
    return GetUserEffectivePermissionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserEffectivePermissionsResponse>, I>>(
    object: I,
  ): GetUserEffectivePermissionsResponse {
    const message = createBaseGetUserEffectivePermissionsResponse();
    message.permissions = object.permissions?.map((e) => EffectivePermission.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
