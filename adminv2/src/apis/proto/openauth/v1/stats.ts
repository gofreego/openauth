// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: proto/openauth/v1/stats.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "v1";

/** StatsRequest represents a request for system statistics */
export interface StatsRequest {
}

/** StatsResponse contains system statistics */
export interface StatsResponse {
  /** Total number of users in the system */
  totalUsers: string;
  /** Total number of permissions in the system */
  totalPermissions: string;
  /** Total number of groups in the system */
  totalGroups: string;
  /** Number of currently active users (users with active sessions) */
  activeUsers: string;
}

function createBaseStatsRequest(): StatsRequest {
  return {};
}

export const StatsRequest: MessageFns<StatsRequest> = {
  encode(_: StatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StatsRequest {
    return {};
  },

  toJSON(_: StatsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StatsRequest>, I>>(base?: I): StatsRequest {
    return StatsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatsRequest>, I>>(_: I): StatsRequest {
    const message = createBaseStatsRequest();
    return message;
  },
};

function createBaseStatsResponse(): StatsResponse {
  return { totalUsers: "0", totalPermissions: "0", totalGroups: "0", activeUsers: "0" };
}

export const StatsResponse: MessageFns<StatsResponse> = {
  encode(message: StatsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalUsers !== "0") {
      writer.uint32(8).int64(message.totalUsers);
    }
    if (message.totalPermissions !== "0") {
      writer.uint32(16).int64(message.totalPermissions);
    }
    if (message.totalGroups !== "0") {
      writer.uint32(24).int64(message.totalGroups);
    }
    if (message.activeUsers !== "0") {
      writer.uint32(32).int64(message.activeUsers);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalUsers = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalPermissions = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalGroups = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.activeUsers = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatsResponse {
    return {
      totalUsers: isSet(object.totalUsers)
        ? globalThis.String(object.totalUsers)
        : isSet(object.total_users)
        ? globalThis.String(object.total_users)
        : "0",
      totalPermissions: isSet(object.totalPermissions)
        ? globalThis.String(object.totalPermissions)
        : isSet(object.total_permissions)
        ? globalThis.String(object.total_permissions)
        : "0",
      totalGroups: isSet(object.totalGroups)
        ? globalThis.String(object.totalGroups)
        : isSet(object.total_groups)
        ? globalThis.String(object.total_groups)
        : "0",
      activeUsers: isSet(object.activeUsers)
        ? globalThis.String(object.activeUsers)
        : isSet(object.active_users)
        ? globalThis.String(object.active_users)
        : "0",
    };
  },

  toJSON(message: StatsResponse): unknown {
    const obj: any = {};
    if (message.totalUsers !== "0") {
      obj.totalUsers = message.totalUsers;
    }
    if (message.totalPermissions !== "0") {
      obj.totalPermissions = message.totalPermissions;
    }
    if (message.totalGroups !== "0") {
      obj.totalGroups = message.totalGroups;
    }
    if (message.activeUsers !== "0") {
      obj.activeUsers = message.activeUsers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatsResponse>, I>>(base?: I): StatsResponse {
    return StatsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatsResponse>, I>>(object: I): StatsResponse {
    const message = createBaseStatsResponse();
    message.totalUsers = object.totalUsers ?? "0";
    message.totalPermissions = object.totalPermissions ?? "0";
    message.totalGroups = object.totalGroups ?? "0";
    message.activeUsers = object.activeUsers ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
